@inject NavigationManager NavigationManager

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo($"login?returnUri={Uri.EscapeDataString(NavigationManager.Uri)}", forceLoad: true);
    }
}
